# ── 紅線：封包一律不進版控 ────────────────────────────────────
# 任何來自公司或客戶網路的擷取檔都不得被提交。預設全擋，
# 只有 tests/fixtures/ 底下、來源與授權都明確的樣本才開白名單。
*.pcap
*.pcapng
*.cap
*.pdml
# 白名單必須用 ** —— 平面 glob 只吃 tests/fixtures/ 底下第一層，
# 每個場景各自一個子目錄（capture + logs + scenario.md）的結構會整個被擋掉。
!tests/fixtures/**/*.pcap
!tests/fixtures/**/*.pcapng
!tests/fixtures/*.pcap
!tests/fixtures/*.pcapng

# 工作用的本機擷取檔放這裡，永不進版控
local/

# ── Python ──────────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# ── macOS ───────────────────────────────────────────────────
.DS_Store

# ── 輸出 ────────────────────────────────────────────────────
out/
*.mmd
!tests/fixtures/*.mmd

# ── web/ 的建置產物 ─────────────────────────────────────────
# app.js / app.css **刻意進版控** —— 它們在 pyproject 的 package-data 裡，
# `pip install telcoladder` 的人不該需要 Node 才能看到介面。
# index.html 則是 Vite 順帶吐出來的殘留物：外殼由 viewer.py 的 app_page()
# 產生（要注入 sid），這份永遠不會被送出（不在 STATIC_TYPES 白名單裡）。
telcoladder/static/index.html

# Internal working notes (kept out of the public repo)
CLAUDE.local.md
