# ── 紅線：封包一律不進版控 ────────────────────────────────────
# 任何來自公司或客戶網路的擷取檔都不得被提交。預設全擋，
# 只有 tests/fixtures/ 底下、來源與授權都明確的樣本才開白名單。
*.pcap
*.pcapng
*.cap
*.pdml
!tests/fixtures/*.pcap
!tests/fixtures/*.pcapng

# 工作用的本機擷取檔放這裡，永不進版控
local/

# ── Python ──────────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# ── macOS ───────────────────────────────────────────────────
.DS_Store

# ── 輸出 ────────────────────────────────────────────────────
out/
*.mmd
!tests/fixtures/*.mmd
