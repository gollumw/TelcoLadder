# 擷取檔是二進位。Git 通常自己偵測得出來（pcap 前幾個位元組就有 NUL），
# 但 Windows 上 core.autocrlf=true 是預設值，GitHub Actions 的 windows runner
# 也是。賭偵測正確等於拿 fixture 冒險 —— 一份被改成 CRLF 的 pcap
# **不會報錯**，只會讓 tshark 讀出垃圾，然後交叉驗證測試莫名其妙地紅。
*.pcap    binary
*.pcapng  binary
*.cap     binary

# ── 建置產物：不參與 diff ──────────────────────────────────
# `web/`（Vite + React）的產物。**原始碼在 `web/src/`，那裡照常 diff。**
#
# 兩個理由，第二個才是重點：
#
# 1. minified bundle 的 diff 是一整行 264 KB，人讀不了。
# 2. **它會把 pre-push 的機密掃描器餵到失效。** 實測本次 Phase 4 那一推：
#    31 個 MEDIUM 全部來自這裡，而且 31 個全是誤報 —— minified JS 裡的
#    數字字面被認成電話號碼（50）、SSN（6）、信用卡（2）。加了這兩行之後
#    同一次推是 0 個。
#
#    **每次動前端就噴幾十筆雜訊，等於訓練人略過那個檢查** —— 然後真的有
#    東西時也不會有人看。掃描器對這個檔案從來沒有提供過真實覆蓋
#    （31/31 誤報、0 真陽性），所以這不是放寬，是把訊號還回去。
#
# 殘留風險，講明白：**建置期烤進 bundle 的機密會因此看不見**
# （Vite 的 `import.meta.env.VITE_*` 是前端最典型的洩漏路徑）。本專案目前
# 一個 `VITE_` 變數都沒有；哪天要加，那個變數本身就是紅線，不要靠這個掃描器擋。
telcoshark/static/app.js   -diff
telcoshark/static/app.css  -diff
